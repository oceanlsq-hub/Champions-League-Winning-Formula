# Champions League Winner Prediction

A data analysis and machine learning project to predict UEFA Champions League winners using historical match data.

## Project Structure
- `src/`: Core analysis and prediction scripts
- `notebooks/`: Jupyter notebooks for data exploration
- `data/`: Raw and processed match data
- `charts/`: Visualization outputs
- `results/`: Model outputs and predictions

## Installation
```bash
pip install -r requirements.txt